# MinimalActorSystem
Minimal actor system for .NET with virtual time, XML-based model compilation, sync/async test modes, and mutable message accumulator. No external dependencies except ILogger. High performance: 2.3M msg/sec, 6KB per actor. Inspired by minimalism principles.
